<?xml version="1.0" encoding="utf-8"?>
<!-- Detailed View Screen: Scrollable list of all gear items -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/dark_background">

    <!-- Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="20dp"
        android:gravity="center_vertical"
        android:background="@color/dark_card">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="🎒 Gear List"
            android:textSize="22sp"
            android:textStyle="bold"
            android:textColor="@color/forest_green"/>

        <Button
            android:id="@+id/btnBackToBase"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🏠 Back to Base"
            android:textSize="13sp"
            android:textColor="@color/dark_background"
            android:backgroundTint="@color/earth_brown"/>

    </LinearLayout>

    <!-- Scrollable gear list — populated dynamically via loop in DetailedViewActivity -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:padding="16dp">

        <LinearLayout
            android:id="@+id/containerGearList"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"/>

    </ScrollView>

</LinearLayout>
